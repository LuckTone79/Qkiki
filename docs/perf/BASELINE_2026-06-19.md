# Backend Performance Baseline - 2026-06-19

## Scope

- Implemented the first execution slice from `docs/CODEX_BACKEND_OPTIMIZATION_EXECUTION_PLAN_2026-06-19.md`.
- Added opt-in `Server-Timing` instrumentation behind `PERF_TRACE=1`.
- Added opt-in web vitals console reporting behind `NEXT_PUBLIC_PERF_TRACE=1`.
- Added route loading boundaries for the main authenticated app segments.

## Instrumented Endpoints

- `GET /api/usage`
- `GET /api/sessions`
- `GET /api/projects`
- `POST /api/projects`
- `GET /api/presets`
- `POST /api/presets`
- `POST /api/workbench/run`
- `GET /api/workbench/runs/[runId]`
- `GET /api/workbench/runs/[runId]/stream`

## Captured Timing Labels

- Shared setup: `auth`, `parse_body`, `schema`, `db_ops`.
- List APIs: `usage_status`, `session_list`, `project_list`, `preset_list`.
- Mutation APIs: `project_create`, `preset_create`.
- Workbench run creation: `stale_runs`, `usage_access`, `session_upsert`, `usage_reserve`, `v2_run_create`, `v2_queue`, `run_create`, `workflow_start`.
- Run status and stream setup: `run_lookup`, `run_refresh`, `v2_snapshot`, `v2_results`, `legacy_results`, `workflow_results`, `session_lookup`, `workflow_readable`, `workflow_tail`.

## Baseline Commands

- `npm install` completed in the isolated worktree and regenerated Prisma Client.
- `node --test src\server\perf\server-timing.test.mjs` passed.
- `npm run lint` passed.
- `npm run build` passed after implementation.
- Build warning: Next.js reports multiple lockfiles because this worktree lives under `.worktrees`; the build still generated 57 static pages successfully.

## Notes

- `Server-Timing` is not emitted unless `PERF_TRACE=1`.
- Web vitals logging is not emitted unless `NEXT_PUBLIC_PERF_TRACE=1`.
- Dynamic sidebar links for individual sessions and projects still opt out of automatic prefetching to avoid background fetch bursts.
